# 手動プレイ用GUI案

目的は、安福さん・長谷川さんが「デッキを作る -> 自分で試しにプレイする -> 気づきをメモする」を回せるようにすること。

## 基本方針

- 公式リプレイ表示はKaggle公式Visualizerを使う。
- 自作GUIは、競技用の内部ツールとして使う。
- 公開配布や公式Visualizer代替のような見せ方は避ける。
- まずは強いUIより、デッキ作成と選択肢理解を助ける最小構成にする。

## 最初に作る機能

### 1. デッキビルダー

- カードID、カード名、種類、HP、タイプ、効果説明で検索
- クリックでデッキに追加
- 60枚チェック
- 同一カード枚数チェック
- `decks/deck_xxx.csv` に保存
- 採用理由メモへのリンクを表示

### 2. 手動プレイ画面

- `cg` シミュレータを使ってローカル対戦を開始
- まずは両プレイヤーを人間が選択
- 次に、片方だけ既存のルールベースAgentにする
- `obs.select.option` を人間向けに表示
- 選んだ行動を `battle_select()` に渡す
- 結果とログを保存

### 3. 振り返り

- 勝敗
- 詰まった場面
- 使わなかったカード
- 強かったカード
- 次に変えるカード

## 実装候補

### Streamlit

利点:

- 早い
- Pythonだけで作れる
- デッキビルダーに向く

弱点:

- 複雑なゲーム画面は作りにくい

### Flask/FastAPI + HTML

利点:

- 自由度が高い
- 手動プレイ画面を作りやすい

弱点:

- 実装量が増える

## 推奨

まずは外部依存なしの `scripts/manual_play_server.py` で手動プレイ画面を作る。

その後、デッキビルダーやカード検索が必要になったら Streamlit または Flask/FastAPI に広げる。

## 現在の実装

手動で両プレイヤーを操作するGUI:

```powershell
python scripts\manual_play_server.py --port 8765
```

ブラウザで開く:

```text
http://127.0.0.1:8765
```

## 注意

- `Competition_Rules.md` は公式原文なので変更しない。
- 競技データやポケモン要素を競技外に公開しない。
- リプレイ表示は公式Visualizerを優先する。
