# GPU dependencies are managed by uv.
# Add optional GPU/training packages to pyproject.toml with:
#   uv add <package>
#
# The default Docker image is a PyTorch CUDA runtime image, so torch is provided by the image.
