# 安福・長谷川向け 作業案内

このプロジェクトでは、ポケモンカードのAIを作ります。

二人にお願いしたいことは、次の2つだけです。

1. ポケカ公式サイトで作ったデッキコードを、このシステムに入れる
2. GUIで実際にプレイして、強そうなデッキを探す

CSVやプログラムは直接触らないでください。

## 触らないもの

次の場所は編集しないでください。

- `agents/`
- `scripts/`
- `submission/`
- `models/`
- `training/`
- `docker/`
- `decks/`
- `API.txt`
- `Competition_Rules.md`

`decks/` の中にCSVができますが、自分で開いて編集しなくて大丈夫です。

## 作業前にやること

毎回、作業を始める前に最新版を取ります。

```bash
git pull
```

## 1. 公式サイトでデッキを作る

ポケカ公式サイトのデッキ構築で60枚デッキを作り、デッキコードを発行します。

例:

```text
4GGxYc-KmW2Iv-8c4c8c
```

## 2. デッキコードをシステムに入れる

次のコマンドを実行します。

```bash
python scripts/import_deck_code.py 4GGxYc-KmW2Iv-8c4c8c
```

自分のデッキコードを使うときは、最後の部分だけ置き換えてください。

成功した場合:

```text
OK decks/local/deck_4GGxYc_KmW2Iv_8c4c8c.csv
```

この場合、そのデッキはGUIで使えます。

失敗した場合:

```text
NG stage=map
ERROR: ...
```

この場合、そのデッキは今のKaggle環境ではそのまま使えません。表示されたエラーを見て、公式サイト側で別のカードに変えてください。

よくある失敗:

- Kaggle環境に存在しないカードが入っている
- 同じ名前のカードが複数あり、どれか自動判定できない
- 60枚になっていない
- たねポケモンが入っていない
- 同名カードを入れすぎている

## 3. GUIでプレイする

次のコマンドを実行します。

```bash
python scripts/manual_play_server.py --port 8765
```

そのあと、ブラウザで次を開きます。

```text
http://127.0.0.1:8765
```

GUIで、さきほど `OK` になったデッキを選んで対戦します。

最初はAI相手ではなく、自分でプレイヤー1とプレイヤー2の両方を操作します。相手側の動きも自分で選んでください。

## 4. 良いデッキができたら送る

良さそうなデッキができたら、助友に次を送ってください。

- デッキコード
- 何が強そうだったか
- どの場面で困ったか
- 次に試したい変更

CSVファイルは送らなくて大丈夫です。デッキコードだけあれば、こちらで再現できます。

## 困ったとき

分からなくなったら、次のどちらかを送ってください。

- 実行したコマンド
- 画面に出た `OK` / `NG` / `ERROR` の文章

`git add`、`git commit`、`git push` は実行しなくて大丈夫です。
