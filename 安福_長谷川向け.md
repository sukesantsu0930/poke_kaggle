# 安福・長谷川向け 作業案内

このプロジェクトでは、ポケモンカードのAIを作ります。

二人には、プログラムを書く作業ではなく、主に次の探索とデッキ作成をお願いしたいです。

- 強そうなデッキ案を考える
- `decks/local/` に60枚のデッキCSVを作る
- 重要そうなカードを調べる
- 対戦で大事そうな判断ルールを見つける
- 先攻・後攻、エネルギー、進化、ベンチ、サイドなどの戦略を整理する

## まず読むもの

- `README.md`
- `COLLABORATION_GUIDE.md`
- `GIT_MINIMUM.md`

## 基本的に触る場所

- `decks/local/`
- `research/deck_notes/`
- `research/card_notes/`
- `research/strategy_notes/`
- `research/rule_findings/`

## 触らない場所

慣れるまでは、次の場所は編集しないでください。

- `agents/`
- `scripts/`
- `submission/`
- `models/`
- `training/`
- `docker/`
- `API.txt`

## デッキ案を書くとき

1. `decks/local/` に60枚のカードIDを書いたCSVを作る
2. `research/deck_notes/` に、そのデッキの狙いを書く
3. どのカードを何枚入れたか、なぜ入れたかを書く
4. 強そうな動きと、弱そうな相手を書く

デッキメモは `research/deck_notes/TEMPLATE.md` をコピーして使ってください。

## デッキCSVの書き方

CSVといっても、難しい表ではありません。1行にカードIDを1つだけ書きます。

例:

```text
1158
721
721
722
...
```

ルール:

- 必ず60行にする
- 1行にカードIDを1つだけ書く
- 余計な文字、カード名、コメントは書かない
- ファイル名は `deck_番号_名前.csv` にする

例:

- `decks/local/deck_002_yasufuku_attack.csv`
- `decks/local/deck_003_hasegawa_stable.csv`

カード名や採用理由はCSVではなく、`research/deck_notes/` のメモに書いてください。

## デッキを作ってGUIで試す流れ

基本の流れは次の通りです。

1. 最新版を取る
2. `decks/local/` にデッキCSVを作る
3. デッキがルール上おかしくないか確認する
4. GUIを起動して、自分で両方のプレイヤーを操作して試す
5. 気づいたことを `research/` にメモする
6. 良いデッキだけ助友に直接送る

## 1. 最新版を取る

作業を始める前に、必ず次を実行してください。

```bash
git pull
```

## 2. デッキCSVを作る

`decks/local/` の中に新しいCSVを作ります。

例:

- `decks/local/deck_002_yasufuku_attack.csv`
- `decks/local/deck_003_hasegawa_stable.csv`

CSVにはカードIDだけを書きます。カード名や理由はCSVに書かず、`research/deck_notes/` に書いてください。

## 3. デッキを確認する

デッキを作ったら、次を実行してください。

```bash
python scripts/check_decks.py
```

`OK` と出たデッキはGUIで使えます。

`ERROR` と出た場合は、60枚になっていない、カードIDが違う、同じカードを入れすぎている、たねポケモンが入っていない、などの可能性があります。

## 4. GUIで試す

次を実行します。

```bash
python scripts/manual_play_server.py --port 8765
```

そのあと、ブラウザで次を開きます。

```text
http://127.0.0.1:8765
```

GUIでは、使いたいデッキを選んで対戦を始めます。

最初はAI相手ではなく、自分でプレイヤー1とプレイヤー2の両方を操作します。つまり、片方のデッキだけでなく、相手側の動きも自分で選びます。

不正なデッキはGUI上で選べないようにしています。表示されない、または選べない場合は、先に `python scripts/check_decks.py` で確認してください。

なお、デッキCSVはコンパイルしません。`decks/` と `decks/local/` に置いたCSVをGUIが直接読みます。

## 5. 試した結果をメモする

対戦して気づいたことは、必ずメモしてください。

デッキ単位のメモ:

- `research/deck_notes/`

カード単位のメモ:

- `research/card_notes/`

判断ルールや戦略のメモ:

- `research/strategy_notes/`
- `research/rule_findings/`

書いてほしいこと:

- どのデッキ同士で試したか
- 先攻と後攻のどちらがよさそうか
- 強かった動き
- 弱かった動き
- 事故った原因
- 次に変えたいカード
- 「この場面ではこうした方がよさそう」という判断ルール

完璧な結論でなくて大丈夫です。あとで見返せるように、試したことと気づいたことを残してください。

## ルールや戦略を見つけたとき

`research/rule_findings/` または `research/strategy_notes/` にメモを書いてください。

例:

- 先攻1ターン目にできること
- エネルギーを誰につけるべきか
- ベンチに出す優先順位
- 進化を急ぐべき条件
- 攻撃するか、準備するかの判断

大事なのは、正解を書き切ることではなく、「こうしたら強そう」という仮説をデッキCSVとメモの両方に残すことです。

## Gitの使い方

二人は、基本的にGitへ変更を送らなくて大丈夫です。

このプロジェクトでは、プログラム本体や共有デッキは助友がGitに反映します。二人はGitから最新版を取って、そのシステムを使ってデッキを試してください。

作業前:

```bash
git pull
```

ローカルで作ったデッキCSVは、`decks/local/` に保存してください。ここに置いたCSVはGit管理しない設定なので、自分のPCだけに残ります。

良いデッキができたら、次の2つを助友に直接送ってください。

- `decks/local/` に作ったデッキCSV
- そのデッキの狙いや試した結果のメモ

助友が確認して、必要なものだけGitに反映します。

念のため変更を確認したいとき:

```bash
git status
```

分からなくなったら、`git status` の画面を送ってください。
