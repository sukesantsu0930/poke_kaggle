# 安福・長谷川向け 作業案内

このプロジェクトでは、ポケモンカードのAIを作ります。

二人には、プログラムを書く作業ではなく、主に次の探索とデッキ作成をお願いしたいです。

- 強そうなデッキ案を考える
- `decks/` に60枚のデッキCSVを作る
- 重要そうなカードを調べる
- 対戦で大事そうな判断ルールを見つける
- 先攻・後攻、エネルギー、進化、ベンチ、サイドなどの戦略を整理する

## まず読むもの

- `README.md`
- `COLLABORATION_GUIDE.md`
- `GIT_MINIMUM.md`

## 基本的に触る場所

- `decks/`
- `research/deck_notes/`
- `research/card_notes/`
- `research/strategy_notes/`
- `research/rule_findings/`

## 触らない場所

慣れるまでは、次の場所は編集しないでください。

- `agents/`
- `scripts/`
- `submission/`
- `models/`
- `training/`
- `docker/`
- `API.txt`

## デッキ案を書くとき

1. `decks/` に60枚のカードIDを書いたCSVを作る
2. `research/deck_notes/` に、そのデッキの狙いを書く
3. どのカードを何枚入れたか、なぜ入れたかを書く
4. 強そうな動きと、弱そうな相手を書く

デッキメモは `research/deck_notes/TEMPLATE.md` をコピーして使ってください。

## デッキCSVの書き方

CSVといっても、難しい表ではありません。1行にカードIDを1つだけ書きます。

例:

```text
1158
721
721
722
...
```

ルール:

- 必ず60行にする
- 1行にカードIDを1つだけ書く
- 余計な文字、カード名、コメントは書かない
- ファイル名は `deck_番号_名前.csv` にする

例:

- `decks/deck_002_yasufuku_attack.csv`
- `decks/deck_003_hasegawa_stable.csv`

カード名や採用理由はCSVではなく、`research/deck_notes/` のメモに書いてください。

## ルールや戦略を見つけたとき

`research/rule_findings/` または `research/strategy_notes/` にメモを書いてください。

例:

- 先攻1ターン目にできること
- エネルギーを誰につけるべきか
- ベンチに出す優先順位
- 進化を急ぐべき条件
- 攻撃するか、準備するかの判断

大事なのは、正解を書き切ることではなく、「こうしたら強そう」という仮説をデッキCSVとメモの両方に残すことです。

## Gitの最低限

作業前:

```bash
git pull
```

変更確認:

```bash
git status
```

共有:

```bash
git add decks research
git commit -m "add deck and strategy notes"
git push
```

分からなくなったら、`git status` の画面を送ってください。
