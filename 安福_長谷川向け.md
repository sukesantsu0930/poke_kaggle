# 安福・長谷川向け 作業案内

このプロジェクトでは、ポケモンカードのAIを作ります。

二人には、プログラムを書く作業ではなく、主に次の探索とデッキ作成をお願いしたいです。

- 強そうなデッキ案を考える
- ポケカ公式サイトでデッキを作り、デッキコードを発行する
- 重要そうなカードを調べる
- 対戦で大事そうな判断ルールを見つける
- 先攻・後攻、エネルギー、進化、ベンチ、サイドなどの戦略を整理する

## まず読むもの

- `README.md`
- `COLLABORATION_GUIDE.md`
- `GIT_MINIMUM.md`

## 基本的に触る場所

- `research/deck_notes/`
- `research/card_notes/`
- `research/strategy_notes/`
- `research/rule_findings/`

## 触らない場所

慣れるまでは、次の場所は編集しないでください。

- `agents/`
- `scripts/`
- `submission/`
- `models/`
- `training/`
- `docker/`
- `API.txt`

## デッキ案を書くとき

1. ポケカ公式サイトのデッキ構築で60枚デッキを作る
2. デッキコードを発行する
3. `research/deck_notes/` に、そのデッキの狙いを書く
3. どのカードを何枚入れたか、なぜ入れたかを書く
4. 強そうな動きと、弱そうな相手を書く

デッキメモは `research/deck_notes/TEMPLATE.md` をコピーして使ってください。

## デッキコードについて

二人はCSVを直接書かなくて大丈夫です。

ポケカ公式サイトでデッキを作ると、次のようなデッキコードが発行されます。

```text
4GGxYc-KmW2Iv-8c4c8c
```

このデッキコードを助友に送ってください。助友側で、Kaggle用のCSVに変換できるかチェックします。

変換できない場合があります。理由は、公式サイトにあるカードがKaggle環境のカードリストに存在しない、または同名カードが複数あって自動判定できないためです。

## デッキを作ってGUIで試す流れ

基本の流れは次の通りです。

1. 最新版を取る
2. 公式サイトでデッキを作る
3. デッキコードを助友に送る
4. GUIを起動して、自分で両方のプレイヤーを操作して試す
5. 気づいたことを `research/` にメモする
6. 改良したらまたデッキコードを送る

## 1. 最新版を取る

作業を始める前に、必ず次を実行してください。

```bash
git pull
```

## 2. デッキコードを送る

ポケカ公式サイトで60枚デッキを作り、デッキコードを発行します。

例:

```text
4GGxYc-KmW2Iv-8c4c8c
```

そのコードと、デッキの狙いを書いたメモを助友に送ってください。

## 3. デッキを確認する

助友が次のようなコマンドで、デッキコードをKaggle用CSVに変換できるか確認します。

```bash
python scripts/import_deck_code.py 4GGxYc-KmW2Iv-8c4c8c
```

`OK` と出たデッキだけGUIで使えるCSVになります。

`NG` と出た場合は、どのカードが変換できなかったか、どのルールに引っかかったかが表示されます。

## 4. GUIで試す

次を実行します。

```bash
python scripts/manual_play_server.py --port 8765
```

そのあと、ブラウザで次を開きます。

```text
http://127.0.0.1:8765
```

GUIでは、使いたいデッキを選んで対戦を始めます。

最初はAI相手ではなく、自分でプレイヤー1とプレイヤー2の両方を操作します。つまり、片方のデッキだけでなく、相手側の動きも自分で選びます。

不正なデッキはGUI上で選べないようにしています。表示されない、または選べない場合は、助友側でデッキコードの変換結果を確認します。

なお、GUIはコンパイル済みのCSVを読みます。二人はCSVを直接編集しません。

## 5. 試した結果をメモする

対戦して気づいたことは、必ずメモしてください。

デッキ単位のメモ:

- `research/deck_notes/`

カード単位のメモ:

- `research/card_notes/`

判断ルールや戦略のメモ:

- `research/strategy_notes/`
- `research/rule_findings/`

書いてほしいこと:

- どのデッキ同士で試したか
- 先攻と後攻のどちらがよさそうか
- 強かった動き
- 弱かった動き
- 事故った原因
- 次に変えたいカード
- 「この場面ではこうした方がよさそう」という判断ルール

完璧な結論でなくて大丈夫です。あとで見返せるように、試したことと気づいたことを残してください。

## ルールや戦略を見つけたとき

`research/rule_findings/` または `research/strategy_notes/` にメモを書いてください。

例:

- 先攻1ターン目にできること
- エネルギーを誰につけるべきか
- ベンチに出す優先順位
- 進化を急ぐべき条件
- 攻撃するか、準備するかの判断

大事なのは、正解を書き切ることではなく、「こうしたら強そう」という仮説をデッキコードとメモの両方に残すことです。

## Gitの使い方

二人は、基本的にGitへ変更を送らなくて大丈夫です。

このプロジェクトでは、プログラム本体や共有デッキは助友がGitに反映します。二人はGitから最新版を取って、そのシステムを使ってデッキを試してください。

作業前:

```bash
git pull
```

デッキは公式サイトで作り、デッキコードを保存してください。CSVは直接編集しません。

良いデッキができたら、次の2つを助友に直接送ってください。

- デッキコード
- そのデッキの狙いや試した結果のメモ

助友が確認して、必要なものだけGitに反映します。

念のため変更を確認したいとき:

```bash
git status
```

分からなくなったら、`git status` の画面を送ってください。
