# サーバー作業手順（gs83 にログインしたらやること）

> **2026-07-17 更新**: 現行の学習トラックは **BC→模倣正則化つき PPO**
> （[docs/strategy/エージェント学習プロセス.md](docs/strategy/エージェント学習プロセス.md) が正典）。
> サーバーの仕事は同文書の **Phase P（PPO）と Phase E の L2 計測**。手順は下の
> 「§P 現行: 模倣正則化つき PPO」を使う。§5〜§8 の CEM 手順は θ 時代のアーカイブ
> （EXP-006 で構造的限界が判明。再利用するのは終盤リフレッシュの発想だけ）。

## §P 現行: 模倣正則化つき PPO（2026-07-17〜）

**前提**（Windows 側で済んでいること = git pull で届く）: ルール成熟（Gate A/B/C 通過）
→ 教師データ再抽出（`data/imitation/<deck>_bc/*.npz`）→ BC（`models/bc_<deck>_vN.npz`）。
環境構築が未了なら下の §0〜§4 を先に（初回のみ）。

```bash
git pull    # train_ppo の --bc-data 系フラグ（07-17）と BC 成果物を取り込む
tmux new -s ppo
docker compose run --rm ptcg uv run python training/train_ppo.py \
    --agent agents/<deck>_rb --deck decks/fleet/<deck>.csv \
    --field research/meta/<最新日付>_field.csv \
    --exclude <holdout3種> \
    --resume models/bc_<deck>_vN.npz \
    --bc-data "data/imitation/<deck>_bc/*.npz" --bc-eval-days <holdout日> \
    --bc-coef 0.3 --bc-coef-final 0.05 \
    --iters 60 --games-per-iter 256 --workers 7 \
    --out build/ppo_bcmix/<deck>
```

- 所要 ≈ 数時間/デッキ（EXP-022 実績: 60iter×256games・workers 7）。デッキ間に依存は
  ないので tmux ウィンドウを分けて並列可（コア数と相談。workers×並列数 ≤ 物理コア-1）。
- **監視**: `build/ppo_bcmix/<deck>/history.csv` の `win_rate` と `bc_holdout`。
  `bc_holdout`（教師 holdout 一致率）が学習中に大きく崩れる = プール過適合 → 報告。
- 終わったら L2 スクリーニング（ミラー除外 80戦/枠。判定基準は §6 と同じ漏斗）:

```bash
docker compose run --rm ptcg bash -lc '
uv run python scripts/gauntlet.py --agent agents/<deck>_rb --deck decks/fleet/<deck>.csv \
  --exclude <holdout3種+自アーキ> --games 10 \
  --net build/ppo_bcmix/<deck>/ema.npz &&
uv run python scripts/gauntlet.py --agent agents/<deck>_rb --deck decks/fleet/<deck>.csv \
  --exclude <holdout3種+自アーキ> --games 10
'
```

- 差なし〜劣化 → kill（LEDGER 1行）。+ 方向 → `--games 20〜40` で確定 + holdout 3種
  （`--only <holdout3種>`）の非劣化確認 → git で ema.npz + history.csv を返す
  （§7 と同じ流儀。**採用判断と提出は Windows 側**）。

---

（以下、θ/CEM 時代のアーカイブ）

対象: 学習キュー4体の θ 獲得（CEM = 制圧度のブラックボックス最適化）。
上から順にコピペしていけば動くように書いてある。詳しい背景は
[docs/planning/サーバーブートストラップ.md](docs/planning/サーバーブートストラップ.md) と
[docs/strategy/エージェントアーキテクチャと実験計画.md](docs/strategy/エージェントアーキテクチャと実験計画.md) を参照。

**前提**: このリポジトリは GitHub に push 済み（コード・エージェント・cg エンジン・
模倣θ候補まで全部入り）。**生エピソード（3GB）の転送は不要**——サーバーの仕事は対戦の
自前生成であって、エピソードは使わない。

**⚠ この手順は 2026-07-09 のコード修正を前提とする。古い checkout では回さないこと**
（`git pull` 必須）。修正内容:

- `optimize_theta.py` の init-scale バグ修正（旧実装は `--init-scale 0` でも top-k 外の
  模倣θ 65 個が固定注入されていた = EXP-002 で reject した値が土台に混入）
- **相手プール凍結**: 相手役の θ は常時空（`gauntlet.py _freeze_opponent_theta`）。
  θ を採用して `agents/<deck>/theta.json` を置いても、相手プールの同デッキは手書き基準の
  まま = 目的関数がこちらの採用判断で動かない（p=0 運用。実験計画 §4）
- `--exclude`（holdout 除外）と `gauntlet --theta`（候補θ注入 / `zero` でベースライン）を追加

---

## 学習方針の要約（なぜこの4体か・何を守るか）

- **キュー**: ① marnie（判定器）→ ② chandelure → ③ dragapult → ④ archaludon。
  選定根拠: chandelure = ラダー922（全機最高）+ ローカル制圧度2位 / archaludon = 制圧度
  70.3%（1位）+ アンカー880 / dragapult = 制圧度61% + 対marnie 80% / marnie = シェア1位
  かつスクリーニングの判定器。**alakazam はキュー外**（制圧度37.2%は θ でなく φ の問題。
  Windows 側で divergence 診断へ）
- **marnie が門番**: marnie のスクリーニング（§6）で手書き θ=0 に正の差が出なければ、
  **以降のキューは全部止めて報告**（reason 空間に伸び代なし = 資源を φ 探索へ回す判断材料）
- **holdout = okidogi, lopunny, rocket**（計10.6%シェア。generic 操縦の小型デッキ =
  「未知の新デッキ」の代理）。学習プールから除外し、採用判定で非劣化を検証する
- 相手は学習させない・self-play にしない（逐次ベストレスポンス。場は外生の固定モデル）

## 0. 前提チェック（初回のみ）

```bash
git --version          # あればOK。なければ: sudo apt install git
docker --version
docker compose version # v2 系であること
ssh -T git@github.com  # "Hi sukesantsu0930!" が出ればOK。
                       # 出なければ ssh-keygen して公開鍵を GitHub に登録
```

`docker` コマンドが permission denied になる場合:
`sudo usermod -aG docker $USER` して一度ログインし直す。

## 1. クローン（初回のみ。2回目以降は `git pull`）

```bash
git clone git@github.com:sukesantsu0930/poke_kaggle.git
cd poke_kaggle
```

## 2. Docker イメージのビルド（初回のみ）

```bash
docker compose build
```

**GPU の注意**: compose は `gpus: all` を要求する。今のフェーズ（CEM）は CPU しか
使わないので、`docker compose run` が GPU 関連エラーで落ちる場合は
`docker-compose.yml` の `gpus: all` の行と `deploy:` ブロックをコメントアウトしてよい
（nvidia-container-toolkit を入れるのは NN を使う Phase 5 からで十分）。

## 3. Python 環境（初回のみ・重要）

イメージに焼き込まれた venv はリポジトリのマウントで隠れるため、初回に一度だけ:

```bash
docker compose run --rm ptcg uv sync
```

`.venv` がリポジトリ直下にできて以後永続する。

## 4. スモークテスト（環境が揃ったことの確認）

```bash
docker compose run --rm ptcg bash -lc '
uv run python -c "import sys; sys.path.insert(0,\"submission\"); from cg import api; print(len(api.all_card_data()))" &&
uv run python scripts/sync_base.py --check &&
uv run python scripts/check_agent.py --agent agents/archaludon_rb --deck decks/fleet/archaludon_cityleague.csv --games 5 &&
uv run python scripts/gauntlet.py --agent agents/marnie_munkidori_rb --deck decks/fleet/winrate_2_marnie_grimmsnarl.csv --games 8
'
```

合格基準（上から順）:
1. 数字（カード総数）が表示される — cg エンジン（libcg.so）ロードOK
2. 何も出ずに終わる — 基盤同期OK
3. `OK all invariants pass`
4. 表と `制圧度` と **`throughput: ... games/min`** が出る — 対戦生成OK

**→ 4 の throughput の数値を `research/experiments/LEDGER.md` の EXP-001 の行に
「サーバー: XXX games/min」と追記してコミットしておく**（実験予算の根拠になる）。
参考: Windows 開発機は 336〜627 games/min。

## 5. CEM 本番（メインの仕事）

### 対象4体の対応表

| # | デッキ | `--agent` | `--deck` | `--init` |
|---|---|---|---|---|
| ① | marnie | `agents/marnie_munkidori_rb` | `decks/fleet/winrate_2_marnie_grimmsnarl.csv` | `training/theta_candidates/marnie_imitation.json` |
| ② | chandelure | `agents/chandelure_rb` | `decks/fleet/chandelure_top.csv` | `training/theta_candidates/chandelure_imitation.json` |
| ③ | dragapult | `agents/dragapult_rb` | `decks/fleet/popular_4_dragapult.csv` | `training/theta_candidates/dragapult_imitation.json` |
| ④ | archaludon | `agents/archaludon_rb` | `decks/fleet/archaludon_cityleague.csv` | `training/theta_candidates/archaludon_imitation.json` |

模倣θ（`--init`）は4体分とも作成済み（2026-07-09、Windows側）。用途は
**「どの reason を動かすか」の選定だけ**（`--init-scale 0` で値は使わない = EXP-002 の教訓）。

### 実行（まず ① marnie。長時間走るので tmux の中で）

```bash
tmux new -s cem
docker compose run --rm ptcg uv run python training/optimize_theta.py \
    --agent agents/marnie_munkidori_rb \
    --deck decks/fleet/winrate_2_marnie_grimmsnarl.csv \
    --init training/theta_candidates/marnie_imitation.json \
    --init-scale 0 --top-k 24 \
    --exclude okidogi,lopunny,rocket \
    --pop 12 --elite 3 --iters 30 --games 24 \
    --out build/cem/marnie
# デタッチ: Ctrl+b → d / 再アタッチ: tmux attach -t cem
```

②〜④ は `--agent` / `--deck` / `--init` / `--out` を対応表の値に差し替えるだけ
（`--out build/cem/chandelure` のように）。

**所要時間の見積もり**: 1候補の評価 = 8マッチアップ × 24 = 192試合。
12候補 × 30反復 = **69,120試合/デッキ ≈ 2〜3.5時間**（336〜627 games/min 換算）。
旧手順書の「30〜60分」は誤り。4体直列で 8〜14時間。**CEM は1プロセス1コアなので、
コアが余っていれば tmux のウィンドウを分けて複数デッキ並列でよい**（デッキ間に依存なし。
ただし ① marnie の門番判定（§6）を先に通すこと——止め時を早く知るため）。

### パラメータの意味（変えるならここ）

- `--init-scale 0`: 模倣θから reason の選定だけ借り、初期値は手書き基準（θ=0）から探索
- `--top-k 24`: 動かす reason の数 = 探索次元。上げるなら `--pop` も上げる（elite 3 で
  高次元ガウスを推定すると早期収束する）
- `--games 24`: 1候補・1対面の試合数。**CEM はノイズの上振れ候補をエリートに選ぶ**ので、
  時間を足すなら `--iters` より先に `--games` を増やす
- `--sigma 2000`: 初期標準偏差（スコア単位）。ソフト選好の桁 ±10^4 の 1/5。上げすぎると
  バンド境界を跨ぐ提案が増え、聖域ガードに破棄されて探索が空振りする
- `--exclude okidogi,lopunny,rocket`: holdout。**変えないこと**（採用判定と対にしてある）
- 途中経過は毎反復 `build/cem/<deck>/theta_best.json` に保存される（落ちても安全）

## 6. スクリーニング判定（各デッキの CEM が終わったら即やる）

CEM の best 値はノイズの上振れを含むので、**独立に測り直す**。候補θとベースライン
（θ=0）を同一条件（holdout 除外・80試合）で比較:

```bash
docker compose run --rm ptcg bash -lc '
uv run python scripts/gauntlet.py --agent agents/marnie_munkidori_rb \
  --deck decks/fleet/winrate_2_marnie_grimmsnarl.csv \
  --exclude okidogi,lopunny,rocket --games 10 \
  --theta build/cem/marnie/theta_best.json &&
uv run python scripts/gauntlet.py --agent agents/marnie_munkidori_rb \
  --deck decks/fleet/winrate_2_marnie_grimmsnarl.csv \
  --exclude okidogi,lopunny,rocket --games 10 \
  --theta zero
'
```

判定（実験計画 §5 の漏斗。80試合 = ±7pt 帯）:

- **候補 ≦ ベースライン** → その θ は kill。LEDGER に1行記録。
  **① marnie でこれが出たら②以降は開始しない**（サーバー作業はここで終了・報告）
- **候補 > ベースライン** → 確定測定に進む: 上と同じ2コマンドを `--games 40`（320試合、
  ±3.5pt）で再実行。**+7pt 以上**で holdout 検証へ:

```bash
# holdout（学習で見ていない相手）への非劣化チェック
docker compose run --rm ptcg bash -lc '
uv run python scripts/gauntlet.py --agent agents/marnie_munkidori_rb \
  --deck decks/fleet/winrate_2_marnie_grimmsnarl.csv \
  --only okidogi,lopunny,rocket --games 30 \
  --theta build/cem/marnie/theta_best.json &&
uv run python scripts/gauntlet.py --agent agents/marnie_munkidori_rb \
  --deck decks/fleet/winrate_2_marnie_grimmsnarl.csv \
  --only okidogi,lopunny,rocket --games 30 \
  --theta zero
'
```

holdout で劣化（−7pt 超）していたら**プール過適合**なので不採用（LEDGER に記録）。
非劣化なら L2（実ラダー）候補 → §7 で Windows へ返す。

## 7. 結果を git で返す（θ は KB なので git でOK）

```bash
# 初回のみ: git の名乗りを設定
git config user.name "sukesantsu0930"
git config user.email "suketomo.tsuyoshi@gmail.com"

cp build/cem/marnie/theta_best.json training/theta_candidates/marnie_cem.json
cp build/cem/marnie/history.csv     training/theta_candidates/marnie_cem_history.csv
git add training/theta_candidates/
git commit -m "CEM: marnie theta_best + screening数値をLEDGERに追記"
git push
```

スクリーニング（§6）の数値（候補/ベースラインの制圧度、holdout の結果）を
`research/experiments/LEDGER.md` に EXP-NNN として1行追記してから push すること。

**採用（`agents/<deck>/theta.json` へのコピー）はサーバーではやらない**。Windows 側で
pull して、提出フロー（`validate_episode.py` 合格 → スカウト枠で実ラダー泳がせ2〜3日）
に乗せる。プール凍結によりθを採用しても他デッキの学習の目的関数は変わらないが、
採用判断そのものは L2 の結果を見る主作業者が行う。

## 8. 終盤のリフレッシュ（8/10〜11 頃にもう一度来る仕事）

θ は数時間で再生成できる消耗品。最終ロック（8/12）直前に、最新の場スナップショット
（`research/meta/` の新しい field CSV）に対して**ウォームスタートで取り直す**:

```bash
# 例: 7月に得た CEM θ を初期値に、σ を絞って短く回す（1〜2時間/デッキ）
docker compose run --rm ptcg uv run python training/optimize_theta.py \
    --agent agents/<deck>_rb --deck <deck.csv> \
    --field research/meta/<最新日付>_field.csv \
    --init training/theta_candidates/<deck>_cem.json \
    --init-scale 1 --sigma 500 --iters 10 \
    --exclude <最新fieldでのholdout3種> \
    --pop 12 --elite 3 --games 24 \
    --out build/cem/<deck>_final
```

対象は最終2体の候補（chandelure / archaludon / dragapult / marnie から、その時点の
E[max] ランキング上位）。判定は §6 と同一。

## 9. トラブルシュート

| 症状 | 対処 |
|---|---|
| `docker compose run` が GPU エラー | §2 の通り compose の gpus/deploy をコメントアウト |
| `ModuleNotFoundError: cg` | §3 の `uv sync` を忘れている / リポジトリ直下以外で実行している |
| `libcg.so` ロード失敗 | `git pull` で submission/cg/ が来ているか確認（5.3MB、git管理済み） |
| `--exclude: unknown archetypes` | field CSV とアーキタイプ名が合っていない（`research/meta/*_field.csv` の archetype 列を確認） |
| 対戦が異常に遅い | 他プロセス確認。CEM は1プロセス1コア（並列はデッキ単位で tmux を分ける） |
| push が拒否される | 先に `git pull --rebase`（Windows 側と同時作業した場合） |

## 参考: Windows 側でやること（サーバーではやらない）

- エピソードDL → `training/extract_decisions.py`（抽出）→ `training/fit_theta.py`
  （模倣θ作成 → `training/theta_candidates/<deck>_imitation.json` に置いて push）。
  生エピソードが必要な作業はすべて Windows 側の担当（4体分は 2026-07-09 作成済み）
- θ の採用（`agents/<deck>/theta.json` 配置）・提出 zip 作成・実ラダー泳がせ（L2）
- **alakazam の φ 診断**（対 marnie 20% / 対 chandelure 10% の divergence を開いて
  述語の欠落を特定する。θ キューには入れない）
