# サーバー作業手順（gs83 にログインしたらやること）

対象: 学習ループ（CEM = θ の勝率最適化）をサーバーで回すための手順。
上から順にコピペしていけば動くように書いてある。詳しい背景は
[docs/planning/サーバーブートストラップ.md](docs/planning/サーバーブートストラップ.md) を参照。

**前提**: このリポジトリは GitHub に push 済み（コード・エージェント7体・cg エンジン・
候補θまで全部入り）。**生エピソード（3GB）の転送は不要**——サーバーの仕事は対戦の
自前生成であって、エピソードは使わない。

---

## 0. 前提チェック（初回のみ）

```bash
git --version          # あればOK。なければ: sudo apt install git
docker --version
docker compose version # v2 系であること
ssh -T git@github.com  # "Hi sukesantsu0930!" が出ればOK。
                       # 出なければ ssh-keygen して公開鍵を GitHub に登録
```

`docker` コマンドが permission denied になる場合:
`sudo usermod -aG docker $USER` して一度ログインし直す。

## 1. クローン（初回のみ。2回目以降は `git pull`）

```bash
git clone git@github.com:sukesantsu0930/poke_kaggle.git
cd poke_kaggle
```

## 2. Docker イメージのビルド（初回のみ）

```bash
docker compose build
```

**GPU の注意**: compose は `gpus: all` を要求する。今のフェーズ（CEM）は CPU しか
使わないので、`docker compose run` が GPU 関連エラーで落ちる場合は
`docker-compose.yml` の `gpus: all` の行と `deploy:` ブロックをコメントアウトしてよい
（nvidia-container-toolkit を入れるのは NN を使う Phase 5 からで十分）。

## 3. Python 環境（初回のみ・重要）

イメージに焼き込まれた venv はリポジトリのマウントで隠れるため、初回に一度だけ:

```bash
docker compose run --rm ptcg uv sync
```

`.venv` がリポジトリ直下にできて以後永続する。

## 4. スモークテスト（環境が揃ったことの確認）

```bash
docker compose run --rm ptcg bash -lc '
uv run python -c "import sys; sys.path.insert(0,\"submission\"); from cg import api; print(len(api.all_card_data()))" &&
uv run python scripts/sync_base.py --check &&
uv run python scripts/check_agent.py --agent agents/archaludon_rb --deck decks/candidates/archaludon_cityleague.csv --games 5 &&
uv run python scripts/gauntlet.py --agent agents/marnie_munkidori_rb --deck decks/candidates/2026-06-30_top5/winrate_2_marnie_grimmsnarl.csv --games 8
'
```

合格基準（上から順）:
1. 数字（カード総数）が表示される — cg エンジン（libcg.so）ロードOK
2. 何も出ずに終わる — 基盤同期OK
3. `OK all invariants pass`
4. 表と `制圧度` と **`throughput: ... games/min`** が出る — 対戦生成OK

**→ 4 の throughput の数値を `research/experiments/LEDGER.md` の EXP-001 の行に
「サーバー: XXX games/min」と追記してコミットしておく**（実験予算の根拠になる）。
参考: Windows 開発機は 336〜627 games/min。

## 5. CEM 本番（メインの仕事）

marnie の例（1デッキ 30〜60分目安）。長時間走るので **tmux の中で**実行推奨:

```bash
tmux new -s cem
docker compose run --rm ptcg uv run python training/optimize_theta.py \
    --agent agents/marnie_munkidori_rb \
    --deck decks/candidates/2026-06-30_top5/winrate_2_marnie_grimmsnarl.csv \
    --init training/theta_candidates/marnie_imitation.json \
    --init-scale 0 --top-k 24 \
    --pop 12 --elite 3 --iters 30 --games 24 \
    --out build/cem/marnie
# デタッチ: Ctrl+b → d / 再アタッチ: tmux attach -t cem
```

パラメータの意味（変えるならここ）:
- `--init ... --init-scale 0`: 模倣θから「どの reason を動かすか」だけ借りて、
  **初期値は手書き基準から**出発（模倣θそのものは弱いことが実測済み = EXP-002）。
- `--top-k 24`: 動かす reason の数（次元）。
- `--games 24`: 1候補の評価試合数/対面。ノイズと時間のトレードオフ。
- 途中経過は毎反復 `build/cem/marnie/theta_best.json` に保存される（落ちても安全）。

他のデッキも回す場合は `--agent` / `--deck` / `--out` を差し替える
（デッキ↔CSV の対応は `training/extract_decisions.py` の DECKS 表を参照）。
模倣θがまだ無いデッキは `--init` の代わりに後述の Windows 側手順で作るか、
`--keys` で reason を直接指定する。

## 6. 結果を git で返す（θ は KB なので git でOK）

```bash
# 初回のみ: git の名乗りを設定
git config user.name "sukesantsu0930"
git config user.email "suketomo.tsuyoshi@gmail.com"

cp build/cem/marnie/theta_best.json training/theta_candidates/marnie_cem.json
cp build/cem/marnie/history.csv     training/theta_candidates/marnie_cem_history.csv
git add training/theta_candidates/
git commit -m "CEM: marnie theta_best (server run)"
git push
```

**採用はまだしない**。Windows 側で pull して L1 確定
（gauntlet 320試合・+7pt 基準・holdout 相手で非劣化）を通ったものだけを
`agents/<deck>/theta.json` にコピーして採用する（実験計画 §5 の漏斗）。

## 7. トラブルシュート

| 症状 | 対処 |
|---|---|
| `docker compose run` が GPU エラー | §2 の通り compose の gpus/deploy をコメントアウト |
| `ModuleNotFoundError: cg` | §3 の `uv sync` を忘れている / リポジトリ直下以外で実行している |
| `libcg.so` ロード失敗 | `git pull` で submission/cg/ が来ているか確認（5.3MB、git管理済み） |
| 対戦が異常に遅い | 他プロセス確認。CEM は1プロセスで1コアしか使わない（並列化は今後の課題） |
| push が拒否される | 先に `git pull --rebase`（Windows 側と同時作業した場合） |

## 参考: Windows 側でやること（サーバーではやらない）

エピソードDL → `training/extract_decisions.py`（抽出）→ `training/fit_theta.py`
（模倣θ作成 → `training/theta_candidates/<deck>_imitation.json` に置いて push）。
生エピソードが必要な作業はすべて Windows 側の担当。
